'use strict';

/**
 * menu-title-list controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::menu-title-list.menu-title-list');
